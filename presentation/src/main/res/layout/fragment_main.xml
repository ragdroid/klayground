<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="model"
            type="com.ragdroid.mvi.main.State"/>
    </data>



    <android.support.v4.widget.SwipeRefreshLayout
        android:id="@+id/refreshLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        bind:refreshing="@{model.pullToRefreshing}">
        <FrameLayout
            android:background="@android:color/white"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:context="com.ragdroid.mvi.main.MainFragment"
            tools:showIn="@layout/activity_main">


            <TextView
                android:layout_gravity="center"
                android:visibility="@{model.emptyStateVisible, default=visible}"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_large"
                android:textStyle="bold"
                android:text="@string/no_characters"/>


            <android.support.v7.widget.RecyclerView
                android:layout_gravity="center"
                android:paddingBottom="@dimen/space"
                android:paddingEnd="@dimen/space"
                android:paddingStart="@dimen/space"
                android:paddingTop="@dimen/space"
                android:clipToPadding="false"
                android:id="@+id/listView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"/>

            <ProgressBar
                android:id="@+id/loader"
                style="@style/Widget.AppCompat.ProgressBar"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:layout_gravity="center"
                android:layout_marginBottom="8dp"
                android:layout_marginEnd="8dp"
                android:layout_marginStart="8dp"
                android:layout_marginTop="8dp"
                android:indeterminate="true"
                android:visibility="@{model.loading, default=visible}"/>
        </FrameLayout>


    </android.support.v4.widget.SwipeRefreshLayout>



</layout>
